<h1>Faça seu login</h1>

<?php

$userId = $form->get('user_id');
$userId->setAttribute('class', 'form-control')
    ->setLabelAttributes(['class' => 'control-label']);

$userPassword = $form->get('user_password');
$userPassword->setAttribute('class', 'form-control')
    ->setLabelAttributes(['class' => 'control-label']);

$submit = $form->get('submit');
$submit->setAttribute('class', 'btn btn-primary');

$form->setAttribute('action', $this->url('login'));
$form->prepare();
?>

<?php if ($messageError): ?>
    <div class="alert alert-danger alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span
                aria-hidden="true">&times;</span>
        </button>
        <strong>Erro!</strong> <?= $messageError; ?>
    </div>
<?php endif; ?>

<div class="row">
    <div class="col-md-6">
        <?php echo $this->form()->openTag($form); ?>

        <div class="form-group">
            <?= $this->formLabel($userId); ?>
            <?= $this->formElement($userId); ?>
        </div>

        <div class="form-group">
            <?= $this->formLabel($userPassword); ?>
            <?= $this->formElement($userPassword); ?>
        </div>

        <?php
        echo $this->formSubmit($submit);
        echo $this->form()->closeTag();
        ?>
    </div>
</div>